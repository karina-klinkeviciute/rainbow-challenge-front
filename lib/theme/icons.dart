import 'package:flutter/material.dart';

// Theme color variables use hex format. (0xff added before the hex code of the
// color makes the color value work in Color() class.). Oxff shows that the
// color has full opacity.

// TO DO: maybe we should use MaterialColor model that accepts tints. Tints can
// very handy in some situations.

class ThemeIcons {
  static const challenges = Icons.star_outline;
  static const challengesJoined = Icons.star_half;
  static const regions = Icons.fmd_good;
  static const profile = Icons.account_circle;
  static const chat = Icons.chat;
  static const qrCode = Icons.qr_code;
  static const shop = Icons.shopping_basket;
  static const about = Icons.favorite;
}
